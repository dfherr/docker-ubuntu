set tabstop=8  " The width of a Tab is the default 8

set shiftwidth=2  " Indents will have a width of 2 spaces

set softtabstop=0 " Number of columns for a tab

set smarttab " make "tab" insert indents instead of tabs at the beginning of a line

set expandtab " Expand tabs to spaces

set number " Line numbers

set mouse=a " Enable mouse for all modes, make sure vim --version has +xterm_clipboard support, e.g. install vim-gnome
